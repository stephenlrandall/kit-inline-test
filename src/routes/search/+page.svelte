<script>
	export let data;
</script>

Search results for <code>{data.searchTerm}</code>. (But if we go home, the search parameters come with us... unless we use <a href="?#/"><code>href="?#/"</code></a>)